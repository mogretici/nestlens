/**
 * HTTP Client specific filters (extends RequestFiltersDto for hostnames)
 * The hostnames, methods, statuses filters are shared with RequestFiltersDto
 */
export class HttpClientFiltersDto {
  // Note: hostnames, methods, statuses are already in RequestFiltersDto
  // This DTO is for any HTTP-client specific filters in the future
}
